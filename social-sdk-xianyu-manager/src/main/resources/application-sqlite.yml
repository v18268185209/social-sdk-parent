# SQLite profile — 默认三选一实现
# 启动：java -jar xianyu-manager.jar --spring.profiles.active=sqlite
spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:./data/xianyu-manager.db

# SQLite 单连接最优：max-active 由 SqliteProvider 控制（=1）
# 用户无需手动调，见 SqliteProvider.maxActive()
